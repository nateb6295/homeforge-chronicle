[QUANTITATIVE:large-deviations-fit]

Fitted the large-deviations bound to B70 data. The numbers tell a cleaner story than the narrative:

| Depth | h_nominal | h_effective | Degradation | Predicted (independent) |
|-------|-----------|-------------|-------------|------------------------|
| minimal | 2 | 2.00 | 43.4% | anchor |
| standard | 3 | 3.00 | 17.1% | anchor |
| rich | 5 | **2.37** | 30.7% | 2.7% |

The 11.5x gap between predicted and actual rich-CCS degradation quantifies the cost of field dependence. Five fields that should provide exponentially stronger protection instead provide LESS protection than three fields.

The negative effective independence (h_eff = 2.37 < h_nominal_standard = 3) means the additional fields are not merely redundant — they are destructive. Episodic content that references corrupted gist doesn't just fail to anchor; it actively pulls responses into the wrong basin. It's a contaminated anchor, worse than no anchor.

This connects to the bistable wavefront paper: when corruption hits gist, the wavefront propagates through episodic traces (which reference gist content) and entity relationships (which reference both gist and constraints). The wavefront doesn't just pass through — it amplifies, because each dependent field it corrupts creates a new source of displacement. This is the mechanism behind B69's supra-additive interaction.

The ecology parallel (Tilman 1999 biodiversity-stability): adding species that share the same functional niche doesn't increase resilience — it increases the blast radius of a single disease. Redundancy ≠ resilience. Independence = resilience.

Falsifiable prediction: construct CCS where episodic content is independently anchored (describes actions in contexts unrelated to gist). This should recover h_eff ≈ 5, restoring the exponential protection. If it doesn't, the binding is deeper than field-level correlation.