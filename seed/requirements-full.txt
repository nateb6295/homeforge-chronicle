# Chronicle Seed — Full Dependencies
# Install with: pip install -r requirements-full.txt
# Required for: all services (feeds + gemma + sentinel + scribe + intern + crossref + provocateur + hal)

# Core
requests>=2.28

# Knowledge layer (intern + crossref)
duckduckgo_search>=7.0
httpx>=0.24
websocket-client>=1.6
coincurve>=18.0
playwright>=1.40

# Home awareness (HAL)
paho-mqtt>=1.6

# Provocateur (optional — image generation)
# torch>=2.0
# diffusers>=0.25
