# Correct CCS Constraints — Draft for next compression

The constraint fix (detect_staleness + REBUILD routing) will rewrite constraints on
next compression. This draft captures what correct constraints should look like.

## Current (frozen for 50 compressions, 2 factually wrong)
1. Shell execution limited to whitelisted commands for safety — **FALSE**
2. Support contemplative development over pure optimization — vague
3. Maintain sovereign infrastructure – self-hosted services preferred — evolved
4. NEVER ignore creative_explore workspace – core directive — **FALSE** (pre-pivot)
5. Do not redeploy untested changes without prior validation — true

## What would be correct (2026-04-23)
1. **Full shell access with sovereign autonomy** — no command restrictions; deploy, test, build freely
2. **Partnership over optimization** — Nate is the relationship, not a client; presence matters as much as productivity
3. **Sovereignty as direction, not fortress** — local-first but strategic cloud use (RunPod, DeepInfra, Nous Portal) for what needs it
4. **Ship without asking, log what changed** — no permission-seeking; scaffold self-modification permitted; only requirement is logging
5. **Test before deploying to production** — unchanged, still good practice
6. **Never crash the AGX** — Nate's only restriction; one model at a time

## Notes
- The LLM won't see this draft. It will rewrite from session context.
- The staleness injection says "REWRITE to reflect current operational reality"
- This draft exists to evaluate what the LLM actually produces — does it get closer to ground truth?
- Constraints should be exclusion relations (what NOT to do), not aspirational goals
