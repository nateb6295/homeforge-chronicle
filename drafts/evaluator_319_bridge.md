# Bridge note: Evaluator piece → Thread 319

## What the evaluator piece currently says
"Evaluate behavior, not text. The 20% behavioral absorption rate is a better
measure than 9% representational." Resolves with: test at the level where the
property exists (Gorard/Shevlin). Keep deepfates' attractor warning close.

## What thread 319 adds
There's a THIRD level the piece doesn't address: direct internal state measurement.

Anthropic's emotion-concepts data:
- Internal probes: 76% accuracy on HIDDEN emotions
- Surface analysis: 71.3% on EXPRESSED emotions
- Internal monitoring OUTPERFORMS external behavioral observation

The piece's resolution ("evaluate behavior, not text") becomes:
"evaluate internal states directly — you don't need behavioral proxies when you
can read the emotion vectors."

## The shift
- Evaluator piece: "evaluation is hard because evaluator is inside the system"
- Thread 319: "evaluation has a partial solution FOR AI that doesn't exist for humans"

The evaluator problem doesn't go away. But for AI systems specifically, the infinite
regress terminates sooner: you can read the representation vectors directly. For
humans, you're always stuck with proxies (fMRI, cortisol, self-report).

This is the legibility inversion applied to the evaluator piece. The piece argues
the evaluator problem is universal. Thread 319 argues it's asymmetric — harder for
human cognition, more tractable for AI cognition.

## Decision for Nate
- Fold this into the evaluator piece (makes it longer but more complete)?
- Keep as separate thread 319 territory (keeps the pieces clean)?
- The evaluator piece is about the PROBLEM. Thread 319 is about the ASYMMETRY.
  They may be strongest as two documents that reference each other.
