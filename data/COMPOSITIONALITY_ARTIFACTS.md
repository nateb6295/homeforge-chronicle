# Compositionality gradient — the seven artifacts

Rescued 2026-08-25 from `shared_artifacts`, a table with 11 rows, no reader and
no writer, which was archived to `/mnt/hdd/chronicle-archive/dead_tables.db`
the same hour. Four of its rows were compressed restatements of `values.md`
(that file is richer — nothing lost). These seven are not values. They are
empirical findings encoded in the **Rules / Examples / Mixed** form, which is
thread **#324 Compositionality Gradient** — an ACTIVE thread.

They sat unread in a dead table while the thread they belong to was live.

## Rules

- Entity guard over-correction damages topology more than under-correction
  (FORGE tau=-11.0 > tau=-1.1, silence probe confirms).
- Constraints are cheap and load-bearing; episodic traces are expensive and
  variable (FORGE Rules/Examples = CCS constraints/episodic).

## Examples

- Capture patterns cluster around sovereignty, neuroscience, and AI identity —
  these are Nate's attention signals.
- Thread advances that cite primary sources (not just summaries) get higher
  engagement from Nate.
- EXTEND moves on #opus posts are most useful when they bring NEW external
  evidence, not restatements.

## Mixed

- The compositionality gradient (Rules/Examples/Mixed) IS the
  disposition/topology axis viewed through depth. Three substrates confirm.
- CCS ablation: gist removal = semantic collapse, episodic removal = anomia,
  constraint removal = ungrounded diversification.

## Status — read this before citing any of the above

These are recovered claims, not verified ones. The FORGE tau figures carry no
runnable method here, which by the standing rule (`data/BASELINES.md`, the
sigma-1 retraction) makes them **history, not baselines** — cite them as prior
belief, never calibrate against them. The "three substrates confirm" line names
no substrates. Treat this file as a pointer back into thread #324, not as
findings that survived review.
